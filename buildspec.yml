version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
    - npm install 
    - npm install @angular/material 
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
    - npm build 
  post_build:
    commands:
    - aws s3 sync ./dist/angular-tour-of-heroes s3://code43-s3-stack-s3bucket-1d8hdif1vfenc
#artifacts:
  #files:
    # - location
    # - location
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths
